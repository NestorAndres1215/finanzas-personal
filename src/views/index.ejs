<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Finanzas Personales</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
    crossorigin="anonymous" />
</head>

<body class="bg-gradient-to-br from-blue-50 to-gray-100 min-h-screen flex items-center justify-center p-4 font-sans">
  <div class="w-full max-w-lg bg-white rounded-xl shadow-2xl p-8 transform transition-all hover:shadow-xl">
    <h1 class="text-3xl font-bold text-center text-gray-800 mb-10 flex items-center justify-center">
      <i class="fas fa-wallet text-indigo-600 mr-3 text-4xl"></i> Finanzas Personales
    </h1>
    <form action="/transacciones" method="POST" class="space-y-5">
      <div class="relative">
        <label class="block text-sm font-semibold text-gray-700 mb-2">
          Tipo
        </label>
        <select name="tipo"
          class="block w-full pl-10 pr-4 py-3 rounded-lg border border-gray-300 bg-gray-50 text-gray-700 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition duration-200 shadow-sm">
          <option value="ingreso">Ingreso</option>
          <option value="gasto">Gasto</option>
        </select>
      </div>

      <div class="relative">
        <label class="block text-sm font-semibold text-gray-700 mb-2">
          Categoría
        </label>
        <input type="text" name="categoria" required
          class="block w-full pl-10 pr-4 py-3 rounded-lg border border-gray-300 bg-gray-50 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition duration-200 shadow-sm"
          placeholder="Ej. Comida, Salario" />
      </div>

      <div class="relative">
        <label class="block text-sm font-semibold text-gray-700 mb-2">
          Monto
        </label>
        <input type="number" step="0.01" name="monto" required
          class="block w-full pl-10 pr-4 py-3 rounded-lg border border-gray-300 bg-gray-50 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition duration-200 shadow-sm"
          placeholder="0.00" />
      </div>

      <div class="relative">
        <label class="block text-sm font-semibold text-gray-700 mb-2">
          Descripción
        </label>
        <input type="text" name="descripcion"
          class="block w-full pl-10 pr-4 py-3 rounded-lg border border-gray-300 bg-gray-50 focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 transition duration-200 shadow-sm"
          placeholder="Detalles (opcional)" />
      </div>

      <button type="submit"
        class="w-full bg-indigo-600 text-white py-3 px-4 rounded-lg hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 transition duration-200 flex items-center justify-center font-semibold">
        <i class="fas fa-save mr-2"></i> Guardar Transacción
      </button>
    </form>
    <hr class="my-8 border-gray-200">
    <div class="grid grid-cols-1 gap-4 sm:grid-cols-2">
      <a href="/transacciones"
        class="flex items-center justify-center bg-indigo-100 text-indigo-700 py-3 px-4 rounded-lg hover:bg-indigo-200 transition duration-200 font-semibold">
        <i class="fas fa-list mr-2"></i> Ver Transacciones
      </a>
      <a href="/transacciones/graficos"
        class="flex items-center justify-center bg-indigo-100 text-indigo-700 py-3 px-4 rounded-lg hover:bg-indigo-200 transition duration-200 font-semibold">
        <i class="fas fa-chart-bar mr-2"></i> Ver Gráficos
      </a>
    </div>
  </div>
</body>

</html>